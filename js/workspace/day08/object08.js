// 은행(Bank)
// 이름(name), 계좌(account)

// 입금(deposit)
// 은행에서 관리하는 유저의 돈(account)이 증가
// 유저의 돈(moeny)이 감소

// 출금(withdraw), 
// 은행에서 관리하는 유저의 돈(account)이 감소
// 유저의 돈(moeny)이 증가

// 계좌이체(transferMoney)
// 은행을 통해 유저1 -> 유저2 입금
// 유저1 돈(account) 감소
// 유저2 돈(account) 증가

// user1(User)
// 이름(name), 돈(moeny)

// user2(User)
// 이름(name), 돈(moeny)